<view class="cu-timeline" style="margin-top:30rpx">
  <view class="cu-item text-pink">
    <view class="bg-pink content shadow">
      <view class="cu-capsule radius">
        <view class="cu-tag bg-white text-pink">活动名称</view>
      </view>
      <view class="margin-top-sm text-content">
        <view>{{taskName}}</view>
      </view>
    </view>
  </view>
  <view class="cu-item text-pink">
    <view class="bg-pink content shadow">
      <view class="cu-capsule radius">
        <view class="cu-tag bg-white text-pink">活动内容</view>
      </view>
      <view class="margin-top-sm text-content">
        <view>{{taskContent}}</view>
      </view>
    </view>
  </view>
  <view class="cu-item text-pink">
    <view class="bg-pink content shadow">
      <view class="cu-capsule radius">
        <view class="cu-tag bg-white text-pink">活动规则</view>
      </view>
      <view class="margin-top-sm text-content">
        <view>{{taskRule}}</view>
      </view>
    </view>
  </view>
  <view class="cu-item text-pink">
    <view class="bg-pink content shadow">
      <view class="cu-capsule radius">
        <view class="cu-tag bg-white text-pink">活动奖励</view>
      </view>
      <view class="margin-top-sm text-content">
        <view>{{taskProfit}}</view>
      </view>
    </view>
  </view>
  <view class="cu-item text-pink" wx:if="{{uploadPic != null && uploadPic != ''}}">
    <view class="bg-pink content shadow">
      <view class="cu-capsule radius">
        <button class="cu-tag bg-white text-pink" bindtap="uploadPic">上传图片</button>
        <view class="cu-tag line-white" bindtap="confirmSubmit">确认提交</view>
      </view>
      <view class="margin-top-sm text-content">
        <view>请点击“上传图片”上传活动图片，然后检查后点击“确认提交”</view>
      </view>
    </view>
  </view>
  <view class="cu-item text-pink" wx:if="{{taskPic != null && taskPic != ''}}">
    <view class="bg-pink content shadow">
      <view class="cu-capsule radius">
        <view class="cu-tag bg-white text-pink">参与情况</view>
      </view>
      <view class="margin-top-sm text-content">
        <view>分数（刷新有一定延迟）：{{score}}</view>
        <view class="task-pic">
          <image src="{{taskPic}}" mode="widthFix" />
        </view>
      </view>
    </view>
  </view>
</view>
